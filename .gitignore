# Dependencies
node_modules/

# Build output (produced by `npm run build` / CI; never committed)
dist/

# Packed tarballs from `npm pack`
*.tgz

# Logs
*.log
npm-debug.log*

# Env / secrets
.env
.env.local
.env.*.local

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# npm auth (NEVER commit)
.npmrc

# tokensave code-graph index (machine-local; Tailwind v4 auto source detection
# scans any non-gitignored file, and this DB holds old source text — leaving it
# unignored resurrected deleted utility classes in dist/styles.css)
.tokensave/
